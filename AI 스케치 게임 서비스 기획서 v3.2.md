# AI 스케치 게임 서비스 기획서 v3.2

> 작성일: 2026-09-22  
> 문서 상태: 논의 내용을 반영한 기획 초안  
> 서비스명: 미정  
> 기준: 기존 v2.0 기획서와 이후 논의

> v3.2 변경: **브라우저 Top-3 분류 → 서버의 속성별 벡터 코사인 유사도 → 속성 가중 합산 → Top-3 가중 평균**으로 점수 구조를 확장했다. 속성 모듈을 추가할 수 있게 하고, 속성 구성·벡터 생성·보정·가중치의 버전을 관리한다. 자동 성공은 종합 점수와 별도로 판단한다. 구체적인 벡터 생성 방법, 가중치, 임계값은 미정이다.

## 1. 문서 목적과 결정 상태

본 문서는 꼬맨틀을 주요 레퍼런스로 하는 싱글 그림 추측 게임의 초기 개발 범위를 정의한다. 게임 규칙, AI 판정, 브라우저와 서버의 역할, 향후 모델 개선에 활용할 데이터 저장 구조를 정리한다.

문서에서 **확정 방향**은 논의에서 합의한 내용, **권장 기본안**은 초안을 구체화하기 위한 제안, **실험 후 결정**은 시제품 검증이 필요한 내용을 뜻한다. 권장안과 실험안을 최종 합의로 간주하지 않는다.

### 1.1 확정 방향

| 항목 | 방향 |
|---|---|
| 서비스 형태 | 웹 기반 싱글 그림 추측 게임 |
| 핵심 레퍼런스 | 꼬맨틀 및 유사도 기반 단어 추측 게임 |
| 멀티플레이 | 초기 서비스 범위에서 제거 |
| 입력 | Pen, Undo, Reset을 제공하는 그림판 |
| 판정 시점 | 사용자가 제출 버튼을 누른 시점 |
| 획·Undo 판정 | 자동 판정하지 않음 |
| 사용자 피드백 | 퍼센트가 아닌 유사도 점수 |
| 프론트 전달 결과 | Top-3 카테고리와 각 모델 점수 |
| 점수 계산 방향 | 후보별 속성 벡터 코사인 유사도를 가중 합산하고, Top-3를 모델 점수에 따라 가중 평균 |
| 속성 구조 | 속성별 벡터·비교·보정·가중치를 독립 모듈로 구성. 초기 권장 축은 분류·형태·기능 |
| 내부 순위 | 후보별 속성 순위를 계산·기록. 혼합 점수에 대응하는 단일 순위와 화면 공개 여부는 추후 결정 |
| 정답 입력 | 텍스트 정답 입력 기능 없음 |
| 성공 | 그림이 성공 조건을 만족하면 자동 성공 처리 후 정답 공개 |
| 초기 학습 데이터 | Quick, Draw! |
| 모델 실행 | 브라우저 추론을 기본 방향으로 설정 |
| 모델 개선 | MLOps는 추후 확장. 초기에는 데이터 수집과 내부 구조를 준비 |

### 1.2 아직 확정하지 않은 사항

- 속성 사전, 벡터 생성 방법, 보정 규칙, 가중치, 점수 범위
- 낮은 확신도에서 판정을 보류할 기준, Top-3 점수 보정 방식, 동순위 처리 규칙
- Top-3 혼합 점수에 대응하는 단일 순위를 제공할지와 그 정의
- 자동 성공 임계값
- AI 예측 단어와 순위의 화면 공개 여부
- 서버에서 성공 후보 그림을 재추론할지 여부
- 그림 수집 대상, 실패 그림 표본 비율, 보관 기간
- 초기 정답 카테고리 목록

## 2. 서비스 개요

### 2.1 한 줄 소개

**그림을 제출하고 유사도 점수를 확인하면서, 숨겨진 오늘의 정답을 찾아가는 싱글 웹 게임.**

사용자는 정답을 모르는 상태에서 그림을 그린다. 그림을 제출하면 AI 분석을 바탕으로 정답과의 유사도 점수가 제공된다. 사용자는 그림을 수정하거나 새로 그리며 탐색하고, 제출한 그림이 성공 조건을 만족하면 정답이 자동으로 공개된다.

### 2.2 핵심 재미

- 무엇을 그릴지 스스로 가설을 세운다.
- 제출 결과를 보고 이전 시도와 비교한다.
- 더 가까워졌다고 생각되는 방향으로 그림을 수정한다.
- 성공 후 자신의 그림과 정답을 비교한다.

그림의 미적 완성도를 평가하는 서비스가 아니다. 점수를 단서로 다음 시도를 선택하는 과정이 게임의 중심이다.

### 2.3 기본 서비스 범위

기존 기획의 **하루 한 문제, PC 웹 중심, 계정 없이 플레이** 구조를 초기 기본안으로 유지한다.

- 데일리 문제를 기본 모드로 제공한다.
- 동일한 문제에서는 모든 사용자가 같은 정답을 탐색한다.
- 장기 랭킹, 친구, 연속 출석 기록은 초기 범위에서 제외한다.
- 모바일 최적화와 추가 게임 모드는 후속 검토 대상으로 둔다.

## 3. 레퍼런스와 적용 방향

| 레퍼런스 | 확인한 특징 | 적용할 요소 |
|---|---|---|
| [꼬맨틀](https://semantle-ko.newsjel.ly/) | 의미 유사도 점수와 가까운 상위 1,000개 단어의 순위를 제공 | 제출·피드백·재추측의 기본 흐름, 이전 시도 비교 |
| [뉴맨틀](https://www.newmantle.kr/) | 단어를 제출하고 더 높은 유사도를 향해 반복 탐색 | 간단한 제출 중심 화면과 국내 사용자 경험 |
| [Semantle](https://semantle.com/) | 의미 유사도와 가까운 단어에 대한 근접 정보를 제공 | 점수를 해석하는 기준과 점진적인 탐색 |
| [Contexto](https://contexto.me/en/daily) | 유사성 순위를 중심으로 표시하며 정답은 1위 | 순위 공개 시 단순한 설명 방식 |
| [카맨틀](https://kamantle.com/) | 점수와 차가움·뜨거움 표현으로 근접도를 전달 | 숫자를 보조하는 색상·상태 표현 |
| [LoLdle Classic](https://loldle.org/classic) | 속성의 일치·부분 일치·불일치를 구분해서 표시 | 여러 속성이 부분적으로 겹치는 대상 관계 설계 |
| [Metazooa](https://metazooa.com/) | 공통 생물 분류를 통해 정답 범위를 좁힘 | 넓은 분류에서 세부 분류로 접근하는 구조 |

주요 레퍼런스는 꼬맨틀로 지정한다. 나머지는 점수 표현과 제출 기록의 사용성을 비교하는 보조 레퍼런스로 활용한다.

점수 설계는 꼬맨틀의 단어 벡터 방식을 복제하지 않는다. 꼬맨틀의 탐색 경험과 속성 비교형 게임의 관계 설계를 결합한다. 속성별 결과를 공개하는 LoLdle과 달리 본 서비스는 초기에는 종합 점수만 공개하므로, 그 점수만으로 탐색 가능한지는 별도로 검증한다.

레퍼런스의 공개된 플레이 규칙을 참고하며, 각 서비스의 비공개 점수 계산 방식이나 모델 성능을 동일하게 재현한다고 가정하지 않는다.

### 3.1 그림 게임으로 바뀌면서 달라지는 점

| 단어 게임 | 본 서비스 |
|---|---|
| 사용자가 단어를 입력 | 사용자가 그림을 제출 |
| 입력 단어가 명확함 | AI가 그림의 대상을 추정해야 함 |
| 이전 단어와 점수를 비교 | 이전 그림과 점수를 비교 |
| 정답 단어와 일치하면 성공 | 그림에 대한 별도 성공 조건이 필요 |

따라서 꼬맨틀의 탐색 경험을 참고하되, **그림 인식 결과와 게임용 유사도를 구분**해서 설계한다.

## 4. 사용자 흐름

1. 사용자가 오늘의 문제에 접속한다.
2. 모델 로딩이 완료되면 그림을 그린다.
3. 필요에 따라 Undo 또는 Reset으로 편집한다.
4. 제출 버튼을 누른다.
5. 제출 순간의 그림을 고정된 사본으로 보관하고 판정한다.
6. 인식 가능한 그림이면 유사도 점수와 제출 기록을 표시한다. 확신도가 부족하면 권장 정책에 따라 판정 보류를 안내한다.
7. 성공하지 못했다면 그림을 수정해 다시 제출한다.
8. 성공 조건을 만족하면 정답과 성공한 그림을 공개한다.

제출 횟수와 플레이 시간은 초기에는 제한하지 않는 것을 권장한다. 이 정책은 무제한 서버 요청을 허용한다는 의미가 아니며, 중복 제출과 비정상 요청에는 별도 제한을 둘 수 있다.

## 5. 그림판과 제출 규칙

### 5.1 그림판

| 기능 | 동작 |
|---|---|
| Pen | 단색 선으로 그림을 추가 |
| Undo | 마지막 획을 취소 |
| Reset | 현재 캔버스를 초기화 |
| 제출 | 현재 그림을 기준으로 판정 요청 |

- 그리기, Undo, Reset 자체로는 AI 판정을 수행하지 않는다.
- Reset을 해도 이전 제출 기록은 유지한다.
- Eraser, 이미지 업로드, 이미지 붙여넣기, 다양한 브러시는 초기 범위에서 제외한다.
- 획은 Undo에 활용할 수 있도록 관리한다. 학습용 저장 시에는 제출 순간의 그림을 복원할 수 있는 데이터로 보관한다.

### 5.2 권장 제출 정책

- 빈 캔버스는 제출하지 못하게 한다.
- 판정 중에는 중복 제출을 막는다. 초기에는 편집도 잠시 잠그는 단순한 방식을 권장한다.
- 변경 없는 동일 그림의 재제출은 기존 결과를 재사용하고 시도 횟수를 늘리지 않는다.
- 정상적으로 판정이 완료된 제출만 시도로 기록한다.
- 권장안으로, 낮은 확신도에 따른 판정 보류는 서버 제출 로그에 남기되 점수가 있는 게임 시도 횟수에는 포함하지 않는다.
- 통신 오류나 추론 오류의 재시도는 시도 횟수에 중복 반영하지 않는다.
- 제출마다 고유 ID를 사용해 재요청을 구분한다.

### 5.3 편집 중 점수 표시

점수는 현재 편집 중인 그림이 아니라 **마지막으로 제출한 그림의 결과**이다.

권장 표시 예시:

> 마지막 제출 유사도: **38.72**  
> 그림이 수정됐어요. 제출하면 다시 확인할 수 있어요.

Reset 후에는 현재 캔버스에 대응하는 점수를 비워두고, 이전 점수는 제출 기록에서 확인할 수 있게 한다.

## 6. 화면 구성

### 6.1 기본 플레이 화면

모바일과 데스크톱을 초기부터 지원한다. 상단에 그림판, 그 아래에 제출 당시 그림 썸네일과 유사도가 누적되는 기록, 하단에 일반 질문·답변 설명글을 배치한다. 반응형 배치와 상태별 화면은 [프론트 화면 계획](docs/frontend-plan-v1.md)을 따른다.

- 오늘의 문제 번호 또는 날짜
- 모델 로딩 상태
- 그림판
- Undo / Reset / 제출 버튼
- 마지막 제출 유사도
- 현재 시도 횟수
- 이전 제출 기록
- 성공 결과 영역

권장 안내 문구:

> 오늘의 정답을 그림으로 찾아보세요.  
> 그림을 제출하면 정답과의 유사도를 알려드려요.

### 6.2 제출 기록

각 시도에는 다음 정보를 표시한다.

- 시도 번호
- 제출 당시 그림 썸네일
- 유사도 점수

기록의 기본 정렬은 최근 제출순을 권장한다. 최고 점수 기록에는 별도 표시를 붙일 수 있다. 과거 그림을 편집 상태로 복원하는 기능은 초기 필수 범위에 포함하지 않는다.

### 6.3 선택적으로 표시할 정보

- AI가 현재 그림을 무엇으로 인식했는지
- 내부 유사도 순위
- 점수를 보조하는 색상이나 차가움·뜨거움 표현

이 항목들은 분석 결과와 화면 표시를 분리해 구현한다. AI 예측 단어와 순위의 공개 여부는 테스트 후 결정한다. 초기 시제품은 화면 옵션으로 켜고 끌 수 있게 한다.

### 6.4 성공 화면

- 정답 단어
- 성공 판정을 받은 그림
- 총 시도 횟수
- 최고 유사도 점수

성공은 제출 결과를 받은 시점에 표시한다. 그림을 그리는 도중이나 Undo 직후에는 자동 성공 판정을 하지 않는다.

포기 후 정답 공개 기능은 막힌 사용자를 위한 권장안으로 두며, 초기 포함 여부는 별도로 결정한다.

## 7. 유사도와 내부 순위

### 7.1 사용자에게 보여줄 점수

- 퍼센트 기호를 사용하지 않는다.
- 기본 명칭은 `유사도`로 한다.
- 예: `유사도 38.72` — 설명용 수치이며 점수 범위가 확정됐다는 의미는 아니다.
- 정답일 확률이나 그림 완성도라고 설명하지 않는다.
- 같은 문제에서 점수가 높을수록 정답에 가깝다는 방향을 일관되게 유지한다.
- 점수 범위, 소수점 자릿수, 보정 방식은 시제품에서 결정한다.
- 화면 점수와 원래 모델 출력은 별도로 저장한다.

### 7.2 채택 구조 — 속성별 벡터 비교와 Top-3 합산

**브라우저는 그림의 Top-3 후보를 분류하고, 서버는 각 후보와 정답의 속성별 벡터를 비교한 뒤 점수를 합산한다.** 사용자는 최종 유사도 점수만 기본적으로 확인한다.

1. 지원 대상마다 분류·형태·기능 등 속성별 정보를 작성한다.
2. 속성별 정보를 각각 벡터로 변환해 저장한다.
3. 프론트가 Top-3 카테고리와 각 모델 점수를 서버로 보낸다.
4. 서버가 후보마다 정답과의 속성별 코사인 유사도를 계산하거나 사전 계산 결과를 조회한다.
5. 속성별 점수를 보정하고 가중 합산해 후보별 관계 점수를 만든다.
6. 후보별 관계 점수를 Top-3 모델 점수에 따라 가중 평균한다.
7. 화면용 점수로 변환해 반환하고, 성공 여부는 별도로 판정한다.

v3.1의 Top-1 고정 관계 점수에서 Top-3 혼합 방식으로 변경한다. 모델 점수는 인식 가능 여부 판단과 후보 간 가중치 계산에 사용한다. 최상위 후보의 관계 점수에 확신도 하나만 곱하는 방식은 사용하지 않는다.

### 7.3 속성별 벡터와 확장 단위

대상마다 속성별로 독립된 벡터를 관리한다. 서로 다른 속성 벡터를 한 번에 연결해 코사인 유사도를 구하지 않고, **같은 속성끼리 비교한 점수들을 합산**한다.

| 속성 모듈 | 정보 예시 | 초기 적용 |
|---|---|---|
| 분류 | 동물 → 포유류, 탈것 → 항공 탈것 | 초기 권장 |
| 형태·구조 | 날개, 바퀴, 길쭉한 몸체, 손잡이 | 초기 권장 |
| 기능·행동 | 비행, 운송, 담기, 자르기 | 초기 권장 |
| 환경 | 수중, 하늘, 주방, 야외 | 후속 추가 후보 |
| 재질·물성 | 금속, 액체, 부드러움 | 후속 추가 후보 |
| 색상 | 대표 색상, 여러 색상 가능 | 후속 추가 후보 |
| 크기·무게 | 대표적인 크기 범주 | 후속 추가 후보 |

글자 수와 철자는 기본 속성에서 제외한다. 첫 시제품은 분류·형태·기능 세 모듈로 시작하는 것을 권장하며, 모듈 구성을 바꿀 수 있게 한다.

속성 벡터는 통제된 태그를 수치화한 특징 벡터 또는 해당 속성 설명을 모델로 변환한 임베딩 벡터로 만들 수 있다. 생성 방법은 시제품에서 결정하며, 같은 모듈 안에서는 모든 대상에 동일한 차원·의미·생성 버전을 사용한다. 속성마다 별도 임베딩 모델을 반드시 학습할 필요는 없다.

속성은 **인식 후보 대상에 사전에 등록한 정보**다. 이미지에서 날개 수나 색상을 별도로 검출하는 구조가 아니다. 특히 흑백 그림판에서 색상 모듈을 추가하면 '현재 그림의 색'이 아니라 '인식 대상의 대표 색'을 비교한다. 색이 일정하지 않은 대상은 별도 적용 규칙이 필요하다.

각 모듈의 등록 규격:

| 구성 | 내용 |
|---|---|
| 식별·설명 | 모듈 ID, 속성 정의, 활성 여부 |
| 데이터·벡터 | 대상별 속성 정보와 벡터, 생성 방법·차원·버전 |
| 비교 | 코사인 유사도를 기본으로 하는 비교 규칙 |
| 보정 | 원래 유사도를 공통 점수 범위로 바꾸는 규칙·버전 |
| 가중치 | 최종 관계 점수에 반영할 비중 |
| 결측 처리 | 미작성, 적용 불가, 영벡터 등 비교 불가능한 경우의 정책 |

### 7.4 점수 계산

#### 1단계 — 속성별 유사도

후보 대상 `c`, 정답 `t`, 속성 모듈 `m`에 대해:

`r_m(c,t) = cosine(v_m(c), v_m(t))`

`s_m(c,t) = 보정_m(r_m(c,t))`

각 보정 결과는 공통 범위인 0~1로 맞추는 것을 권장한다. 모든 모듈이 코사인 유사도를 사용해도 실제 점수 분포는 다를 수 있으므로 분포를 비교한다. 보정은 같은 버전에서 고정되고 원래 유사도의 대소관계를 유지하도록 설계한다. 제출마다 보정 기준을 바꾸지 않는다.

#### 2단계 — 후보별 관계 점수

`R(c,t) = Σ [속성 가중치_m × s_m(c,t)]`

활성 속성의 가중치는 0 이상이고 합이 1이 되게 한다. 예를 들어 분류 0.3, 형태 0.4, 기능 0.3으로 시작할 수 있으나 이는 계산 설명용이며 확정값이 아니다.

#### 3단계 — Top-3 혼합 점수

Top-3 카테고리를 `c1, c2, c3`, 모델 점수를 `p1, p2, p3`라고 할 때:

`q_i = p_i / (p1 + p2 + p3)`

`혼합 점수 = q1 × R(c1,t) + q2 × R(c2,t) + q3 × R(c3,t)`

모델 점수 `p_i`는 전체 카테고리 점수의 합이 1이 되도록 정규화한 비음수 출력값을 사용한다. 로짓을 그대로 가중치로 쓰거나 프론트에서 Top-3만 합계 1로 바꿔 보내지 않는다. 서버가 원래 Top-3 점수의 합을 확인한 후 후보 가중치를 계산한다. 출력값을 실제 정답 확률이라고 보장하지 않는다.

모델 점수 보정을 도입한다면 Top-3를 추출하기 전 전체 출력에 적용하는 등 일관된 규칙을 정하고, 보정 버전도 기록한다.

카테고리를 대표 ID로 통합할 때는 전체 원본 출력을 보정·정규화한 뒤 대표 ID별 확률을 합산하고 Top-3를 추출한다. 이때 p는 합산 값이다. 미지원 후보도 확률과 함께 유지하며 마스킹·재정규화하지 않는다. Top-3에 미지원 후보가 있으면 판정을 보류한다. 매핑·동점 순서·버전 규칙은 [카테고리 정리](docs/catalog-curation-v1.md)와 [API 계약](docs/api-contract-v1.md)을 따른다.

#### 4단계 — 화면 점수

혼합 점수를 화면용 범위로 변환하고 표시 자릿수에 맞춘다. 혼합 점수와 화면 점수는 별도로 저장한다. 최종 표시 범위와 반올림 규칙은 미정이며 퍼센트 기호는 사용하지 않는다.

설명용 예시:

| 정답: 기린 | 모델 점수 | 속성 가중 합산 후 관계 점수 |
|---|---:|---:|
| 말 | 0.50 | 0.70 |
| 기린 | 0.30 | 1.00 |
| 사슴 | 0.15 | 0.80 |

`혼합 점수 = (0.50×0.70 + 0.30×1.00 + 0.15×0.80) / 0.95 ≈ 0.81`

모든 수치는 설명용이다. 이 경우 정답이 Top-1이 아니므로 점수가 높더라도 성공은 아니다. 후보 벡터를 먼저 평균한 뒤 코사인 유사도를 구하는 방식과는 다르며, 본 버전은 **후보별 비교 점수를 계산한 뒤 평균**한다.

### 7.5 결측·불확실성·점수표 관리

- 초기 핵심 모듈은 지원 대상 전체의 정보를 작성한 뒤 발행한다. 미작성은 '특징 없음'과 구분한다.
- 영벡터에는 코사인 유사도를 계산하지 않는다. 적용 불가나 결측을 자동으로 높은 점수 또는 0점으로 치환하지 않는다.
- 후속 모듈의 적용 불가 처리 후보는 해당 비교에서 제외하고 남은 속성 가중치를 재정규화하는 방식이다. 이를 채택하면 실제 사용한 모듈과 가중치를 기록하고, 제외 때문에 점수가 부풀지 않는지 검증한다. 유효한 비교 축이 부족하면 판정을 보류한다.
- Top-3 합이 작거나 최상위 점수가 낮은 경우는 재정규화하기 전 인식 가능 여부를 확인한다. Top-3 합이 0인 요청은 혼합 계산을 하지 않는다. 구체 기준은 검증 데이터로 정한다.
- Top-3 경계의 후보가 바뀔 때 점수가 튈 수 있으며, 수정할수록 점수가 항상 올라가는 것은 아니다.
- 분류 단계의 넓은 특징과 세부 특징, 여러 모듈에 반복 등장하는 특징을 중복으로 과도하게 가산하지 않도록 검토한다.
- 속성별 대상 간 유사도와 후보별 관계 점수는 사전에 계산할 수 있다. 제출 때마다 임베딩 모델을 실행할 필요는 없다.
- 모델 버전과 속성 점수 체계는 독립적으로 관리한다. 점수 체계를 조정하기 위해 분류 모델을 반드시 재학습하지 않는다.

같은 Top-1이 유지되어도 Top-3 구성이나 상대 비중이 바뀌면 최종 점수는 달라질 수 있다. 반대로 같은 후보와 같은 비중이라면 같은 점수가 나온다.

권장 도움말:

> AI가 그림을 보고 떠올린 후보들의 특징을 비교해 유사도를 계산해요. 그림을 수정하고 다시 제출해보세요.

판정 보류 시 권장 안내:

> 아직 어떤 그림인지 알아보기 어려워요. 특징을 조금 더 그려주세요.

### 7.6 내부 순위의 의미

내부 순위는 사용자들 사이의 랭킹이 아니다. **오늘의 정답과 모든 지원 대상을 후보별 관계 점수 R로 정렬했을 때, Top-3 각 후보가 차지하는 순위**를 계산·기록한다.

정답 자체는 별도 일치 규칙으로 1위에 둔다. 나머지는 관계 점수로 정렬하고, 같은 점수는 공동 순위로 처리하는 것을 권장한다. 분류 모델의 1·2·3위와 속성 관계 순위는 별도 항목이다.

Top-3 혼합 점수는 하나의 대상 점수가 아니므로 최상위 후보의 관계 순위를 혼합 점수의 순위라고 표시하지 않는다. 순위를 가중 평균해 최종 순위로 사용하는 것도 기본안에 포함하지 않는다. 혼합 점수에 대응하는 단일 순위의 필요성과 정의는 추후 결정한다.

공동 순위·반올림 규칙, 후보 목록, 비교 대상 수, 점수 체계 버전을 기록한다. 사용자 공개는 보류한다.

### 7.7 속성 모듈 추가 절차

1. 추가 속성의 의미와 적용 가능한 대상을 정한다.
2. 대상별 데이터와 벡터를 만들고 결측을 확인한다.
3. 속성별 코사인 유사도 분포를 확인하고 보정 규칙을 정한다.
4. 새 속성과 기존 속성의 가중치를 함께 조정해 합을 1로 맞춘다.
5. 가까운 후보 목록, 대표 그림 점수, 동점 분포를 기존 버전과 비교한다.
6. 속성 구성·벡터·보정·가중치·점수표를 새 점수 체계 버전으로 발행한다.

벡터를 추가하기만 하면 점수가 자동으로 적절해진다고 가정하지 않는다. 새 기준은 새 문제부터 적용하고 진행 중인 데일리 문제는 기존 버전을 유지한다.

## 8. 자동 성공 판정

### 8.1 확정 방향

사용자가 정답 단어를 직접 입력하지 않는다. 제출 그림이 성공 조건을 만족하면 서버가 성공을 응답하고 정답을 공개한다.

### 8.2 초기 권장 조건

1. 그림의 최상위 예측 카테고리가 오늘의 정답과 일치한다.
2. 해당 카테고리의 모델 점수가 설정한 기준 이상이다.

필요성이 확인되면 1위와 2위 점수 차이를 추가 조건으로 검토한다. 임계값은 검증용 그림으로 정하며, 현 단계에서 임의의 숫자를 확정하지 않는다.

Top-3 혼합 점수만으로 성공시키지는 않는다. 정답이 Top-2나 Top-3에 포함된 것만으로도 성공이 아니다. 최상위 카테고리 ID가 정답과 일치하고 인식 기준을 만족해야 한다. 모든 속성이 같은 다른 대상도 자동 성공시키지 않는다.

낮은 확신도는 먼저 판정 보류로 처리하고, 성공 조건은 전체 카테고리 기준의 모델 점수 p로 확인한다. Top-3 내부에서 다시 정규화한 후보 비중 q를 성공 확신도로 사용하지 않는다. 관계 점수가 매우 높아도 성공이 아닐 수 있음을 구분한다. 성공 전용 최고점을 도입한다면 성공 확정에만 사용하고 실제 혼합 점수와 별도로 기록한다.

브라우저 모델의 점수를 정확한 정답 확률로 간주하지 않는다. 빈 그림, 단순한 선, 다른 카테고리 그림을 잘못 성공시키는지 확인한다.

매 획 판정과 연속 프레임 판정이 없으므로 기존 멀티플레이의 Stable Prediction, inference tick, 동점 규칙은 적용하지 않는다.

## 9. AI 데이터와 브라우저 모델

### 9.1 초기 학습 데이터

[Quick, Draw! 공식 데이터](https://github.com/googlecreativelab/quickdraw-dataset)는 345개 카테고리의 약 5천만 개 그림을 제공하며, 획 데이터와 비트맵 데이터를 사용할 수 있다.

- 처음부터 전체 데이터를 모두 학습해야 하는 것은 아니다.
- 학습 후보 목록과 실제 데일리 정답 목록을 구분한다.
- 정답은 모델이 지원하는 카테고리 중 인식 품질이 확인된 항목에서 고른다.
- 점수가 반환되는 모든 지원 카테고리에 속성 데이터를 작성한다. 데일리 정답 목록에 없더라도 지원 대상이면 점수표가 필요하다.
- 속성 정의가 모호하거나 장면·복합 개념인 카테고리는 초기 지원 목록에서 제외하거나 별도 기준을 정한다. 출시 대상은 그림 인식과 속성 데이터가 모두 준비된 목록으로 관리한다.
- 영문 카테고리 ID와 사용자에게 보여줄 한국어 이름을 매핑한다.
- 동의어 표기와 카테고리 중복·유사성은 목록 관리 단계에서 정리한다.

현재 목록 관리 초안은 [카테고리 정리 v1.1](docs/catalog-curation-v1.md)이다. 모델 원본 345개는 유지하고, 확률 합산 후 인식 후보 335개 중 점수 지원 초안 334개·데일리 후보 323개를 구분한다. `bird`는 미지원 인식 후보로 남긴다. 부분·하위 종류를 대표 정답으로 인정하는 규칙도 출시 전 검증하며, 태그가 비슷하다는 이유만으로 별개 개념의 성공을 인정하지 않는다.

### 9.2 모델과 전처리

학습은 PyTorch 기준으로 설계하고 실행은 ONNX Runtime Web을 우선한다. 작은 28px CNN으로 연결을 확인한 뒤, 64px MobileNetV3-Small을 첫 배포 후보로 두고 Quick, Draw! 공식 분류 예제 기반 Conv1D-BiLSTM과 비교한다. 원본 획 규격·345개 출력·학습/평가·모바일 기준은 [모델 구조](docs/model-architecture-v1.md)를 따른다. 최종 배포 모델은 실제 인식 품질과 기기 성능 측정 후 확정한다.

학습은 별도 개발·학습 환경에서 수행하고, 브라우저에는 추론용 모델을 배포한다. ONNX Runtime Web은 WASM과 WebGPU 실행 경로를 제공한다. 초기에는 작은 모델의 WASM 실행을 우선 확인한다. [공식 실행 안내](https://onnxruntime.ai/docs/tutorials/web/)

학습과 실제 제출에 동일한 전처리 기준을 적용한다.

- 그림 영역 추출과 여백
- 크기 조정과 중앙 정렬
- 선 굵기와 배경·전경 색상
- 픽셀 정규화

모델 다운로드 크기와 추론 시간은 실제 대상 기기에서 측정한다. 학습 결과 없이 목표 성능 달성을 전제하지 않는다.

### 9.3 버전 관리

모델, 전처리, 카테고리 목록, 모델 출력 보정, 성공 조건의 버전을 구분한다. 별도의 **점수 체계 버전**에는 활성 속성 목록, 속성 데이터·벡터 생성, 유사도 보정, 속성 가중치, 결측 처리, Top-3 혼합 규칙, 화면 변환과 사전 점수표를 묶는다.

문제 ID가 사용할 버전을 가리키게 하고 한 문제 진행 중에는 고정한다. 벡터나 가중치를 바꾸면 해당 점수표도 다시 생성한다. 속성 점수 조정은 분류 모델 재학습과 분리한다.

새 모델을 배포해도 진행 중인 문제의 판정 기준이 바뀌지 않게 한다. 브라우저가 호환되지 않는 버전으로 요청하면 재로딩 안내 등 명시적인 처리를 한다.

## 10. 브라우저와 서버의 역할

아래는 정답을 서버에 두고 브라우저에서 추론하기 위한 **권장 구현안**이다.

| 구성 요소 | 역할 |
|---|---|
| 브라우저 | 그림판, Undo·Reset, 제출 사본 생성, 전처리, 분류 모델 추론, Top-3 추출, 결과 표시 |
| 게임 서버 | 데일리 문제 관리, 속성별 벡터·점수표 조회, 속성 합산과 Top-3 혼합, 후보별 순위, 성공 판단, 기록 접수 |
| 그림 저장소 | 수집 대상으로 선정된 원본 그림 또는 복원 가능한 획 데이터 보관 |
| 기록 저장소 | 제출 결과, 버전, 세션, 검수 상태 등 보관 |
| 모델 배포 영역 | 브라우저가 다운로드할 모델과 버전 정보 제공 |

### 10.1 제출 처리

1. 브라우저가 제출 순간의 그림을 고정한다.
2. 로컬 분류 모델로 전체 원본 출력의 보정·정규화된 점수를 계산하고 대표 ID별로 확률을 합산한 뒤 미지원 후보까지 포함해 Top-3를 추출한다.
3. 서버로 문제 ID, 제출 ID, 모델·전처리·카테고리 매핑·출력 보정 버전, Top-3 후보 ID와 각 점수 p를 보낸다.
4. 서버가 문제에 연결된 정답, 점수 체계 버전, 판정 기준을 조회한다. 속성 벡터와 정답은 서버에서 관리한다.
5. 모델 점수, Top-3 합 등으로 인식 가능 여부를 확인한다. 판정 보류이면 혼합 점수를 만들지 않는다.
6. 후보마다 속성별 코사인 유사도 또는 사전 계산 결과를 가져와 보정하고, 속성 가중 합산으로 관계 점수 R을 만든다.
7. Top-3 점수를 후보 가중치 q로 변환하고 관계 점수를 가중 평균한다. 후보별 관계 순위도 기록한다.
8. 혼합 점수를 화면용 점수로 변환한다. Top-1 일치와 인식 기준으로 성공 여부를 별도로 확인해 응답한다.
9. 수집 대상으로 선정된 그림을 제출 ID와 연결해 보관한다.

초기 요청은 전체 분류 점수 배열 대신 Top-3를 기본으로 한다. 각 점수 p는 전체 카테고리 기준으로 정규화한 값을 유지한다. 필수 검사에는 중복·미지원 카테고리 ID, 유한하지 않은 수, 음수·범위 오류, 정렬 순서, 허용 오차를 넘는 Top-3 합, 버전 불일치가 포함된다. 형식 검사는 모델 출력의 진위까지 검증하는 것은 아니다.

제출마다 서버에 결과를 보내는 구조를 유지한다. 정답과 거리가 먼 인식 가능한 대상에도 점수 피드백을 제공한다. 낮은 확신도의 그림에는 권장 정책에 따라 판정 보류를 반환하고, 로그를 남겨 인식 실패를 분석한다. 성공 가능성이 높은 제출만 서버에 보내는 구조는 사용하지 않는다.

### 10.2 응답 정보

- 제출 ID
- 화면용 유사도 점수
- 판정 상태: 인식 완료 / 판정 보류 / 성공. 판정 보류이면 유사도와 게임 순위는 비워둔다.
- 성공 여부
- 성공한 경우 정답 단어
- 필요하면 AI 예측 단어와 공개 정책에 맞는 후보별 순위

후보별 순위와 속성별 기여도는 초기에는 내부 기록으로 보관할 수 있다. Top-3 혼합 점수의 단일 공개 순위는 아직 정의하지 않았다. AI 예측 단어의 화면 표시 여부는 옵션으로 관리한다.

### 10.3 서버 검증의 수준

클라이언트가 보낸 점수는 조작 가능하다. 서버가 그 점수를 정답과 비교했다는 사실만으로 실제 그림을 독립적으로 검증했다고 보지 않는다.

- **단순 MVP안:** 요청 형식·문제·버전을 확인하고 브라우저 결과로 게임 판정한다. 경쟁 랭킹이 없는 초기 서비스에서 허용할지 결정한다.
- **추가 검증안:** 성공 후보에 한해 그림을 서버로 받아 재추론한다.

서버 재추론의 초기 포함 여부는 미정이다. 같은 모델의 재추론은 클라이언트 결과 확인에 도움이 되지만, 그 결과를 검수된 학습 라벨로 만들지는 않는다.

### 10.4 오류 처리

- 모델 로딩·추론 실패 시 재시도할 수 있게 안내한다.
- 서버 요청 실패 시 그림을 유지한다.
- 재요청으로 시도 기록이 중복 생성되지 않게 한다.
- 학습용 보관 실패는 별도 기록하며, 이미 확정된 게임 성공을 취소하지 않는다.

## 11. MLOps 확장을 위한 초기 데이터 구조

### 11.1 초기 목표

초기 버전은 자동 재학습 시스템을 구현하지 않는다. 추후 데이터를 정제·검수하고 재학습할 수 있도록 **원본과 당시 판정 맥락을 남기는 것**을 목표로 한다.

한국 사용자 또는 실제 서비스 환경의 그림이 기존 데이터와 다를 수 있다는 가설은 후속 분석 대상으로 둔다. 차이의 원인을 국적이나 문화로 미리 단정하지 않는다.

### 11.2 수집과 검수의 정의

- **수집:** 제출한 그림과 당시 모델의 판단을 저장하는 작업.
- **검수:** 향후 학습에 사용하기 전에 그림에 붙일 실제 카테고리가 적절한지 확인하는 작업.

예를 들어 오늘의 정답이 기린이고 모델도 기린이라고 판단했더라도, 사람이 보기에는 말 그림일 수 있다. 이 경우 게임 성공 기록은 유지하되, 학습 라벨을 기린으로 자동 확정하지 않는다.

**게임 정답, 모델 예측, 검수 라벨은 서로 다른 항목이다.** 실패한 그림에도 오늘의 정답을 실제 라벨로 붙이지 않는다.

### 11.3 수집 범위 — 권장안

- 학습용 보관을 선택한 사용자의 성공 그림을 수집한다.
- 실패 그림은 일부를 표본으로 수집할 수 있게 한다.
- 동일 제출과 중복 그림의 중복 보관을 줄인다.
- 게임 이용과 학습용 그림 보관 선택을 구분한다.
- 표본 비율, 저장 용량, 보관 기간, 삭제 방법은 초기 운영 규모에 맞춰 정한다.

성공 그림만 수집하면 기존 모델이 잘 알아보는 그림에 데이터가 치우칠 수 있으므로, 실패 표본을 받을 수 있는 구조는 준비한다. 초기부터 모든 그림을 상시 검수하지 않는다.

### 11.4 최소 저장 항목

| 묶음 | 항목 | 용도 |
|---|---|---|
| 제출 식별 | 제출 ID, 임시 세션 ID, 시도 번호, 제출 시각 | 중복 요청과 같은 세션의 수정 그림 연결 |
| 문제 맥락 | 문제 ID, 정답 카테고리 ID | 당시 게임 조건 확인 |
| 그림 | 원본 이미지 또는 복원 가능한 획 데이터, 캔버스·펜 설정, 그림 해시 | 재전처리, 재학습, 중복 확인 |
| 모델 결과 | Top-3 카테고리 ID와 원래 모델 점수 p, Top-3 합, 후보 가중치 q, 판정 상태, 성공 여부 | 인식 결과와 혼합 비중 재현 |
| 점수 내역 | 후보별·속성별 유사도와 보정 점수, 실제 적용 가중치, 후보 관계 점수 R, 후보별 관계 순위, 혼합 점수, 화면 점수 | 어떤 후보와 속성이 결과에 기여했는지 분석 |
| 버전 | 모델, 전처리, 카테고리 목록, 출력 보정, 점수 체계, 성공 조건 버전 | 결과 재현과 비교 |
| 수집 상태 | 학습용 보관 선택, 수집 사유, 저장 완료 여부 | 활용 대상 구분 |
| 검수 확장 | 검수 상태, 검수 라벨, 검수 시각·출처 | 후속 라벨 확인 |

미검수 자료는 검수 상태를 `미검수`, 검수 라벨을 비어 있는 값으로 저장한다. 원시 기록은 보존하고, 향후 확인한 라벨은 별도 관리한다.

전체 분류 출력 저장은 초기 필수가 아니다. 필요한 실험에서 추가 수집할 수 있으며, Top-3 밖의 정보는 해당 로그만으로 복원할 수 없다는 점을 구분한다. 속성별 점수 내역은 재학습 정답 라벨이 아니다.

화면용 작은 썸네일만 학습 원본으로 저장하지 않는다. 같은 세션의 연속 수정 그림이 연결되어야 후속 학습·평가 데이터 분리 시 유사한 그림의 중복 유입을 줄일 수 있다.

### 11.5 후속 확장

1. 검수 대상 추출과 라벨 확인 도구
2. 데이터 정제·중복 제거·데이터셋 버전 관리
3. 검수된 사용자 데이터와 기존 데이터로 재학습
4. 독립 평가 데이터로 기존 모델과 비교
5. 평가를 통과한 모델의 배포와 이전 버전 복구
6. 성능 변화 관찰과 재학습 자동화

초기 출시의 완료 조건에 위 기능들을 포함하지 않는다.

## 12. 초기 평가와 시제품 검증

### 12.1 기술 지표

초기에는 다음 세 가지를 중심으로 확인한다.

| 지표 | 확인할 내용 |
|---|---|
| 그림 인식 정확도 | 별도로 준비한 검증 그림의 카테고리를 올바르게 분류하는가 |
| 잘못된 성공 판정 비율 | 정답이 아닌 것으로 확인한 검증 그림 중 성공으로 처리한 비율 |
| 제출 응답 시간 | 제출부터 결과 표시까지 걸리는 시간. 모델 최초 로딩은 따로 기록 |

검증 그림에는 실제 그림판으로 그린 그림과 빈 그림·낙서·혼동하기 쉬운 카테고리를 포함한다. 기술 지표의 합격 수치는 첫 측정 후 정한다.

사용자 로그의 게임 성공률은 정답 라벨이 검수된 정확도와 다르므로 같은 지표로 취급하지 않는다.

### 12.2 플레이 검증 질문

- 점수와 제출 기록을 보고 다음에 무엇을 그릴지 생각할 수 있는가?
- 그림을 수정한 뒤 점수가 바뀐 결과를 납득하는가?
- 성공 후 공개된 정답과 자신의 그림이 어울린다고 느끼는가?
- AI 예측 단어를 보여줄 때와 숨길 때 어느 쪽이 탐색하기 좋은가?

속성별 코사인 유사도의 분포와 보정 결과를 확인하고, 정답별 가까운 후보 목록과 동점 분포를 검토한다. 대표 문제 몇 개로 낮은 점수에서 출발해 더 가까운 대상을 찾을 수 있는지 테스트한다.

Top-1이 같아도 후보 비중에 따라 점수가 바뀌는 경험이 납득 가능한지 확인한다. Top-3 경계에서 점수가 갑자기 바뀌는지, 모호한 그림이 여러 후보에 걸쳐 높은 점수를 얻는지도 확인한다. 벡터 누락·영벡터·선택 모듈 적용 불가를 처리할 때 점수가 부당하게 높아지지 않는지 점검한다.

숫자만으로 탐색이 막힌다면 우선 속성 가중치와 인식 단어 공개 옵션을 비교한다. 성능 목표를 늘리기보다 대표 사례의 속성별·후보별 기여도를 확인해 원인을 찾는다.

## 13. 데일리 운영 기본안

- 하루에 하나의 정답을 제공한다.
- 갱신 시각은 한국 시간 자정을 권장한다.
- 문제 ID에 정답, 모델, 전처리, 점수 기준, 성공 조건 버전을 연결한다.
- 자정을 넘겨 진행하는 세션은 시작한 문제를 유지하고, 새 문제는 별도로 시작하게 하는 방식을 권장한다.
- 제출 기록과 성공 여부는 동일 브라우저에서 이어 볼 수 있는 로컬 저장 방식을 우선 검토한다.
- 계정 간 동기화, 영구 전적, 연속 출석 기록은 초기 필수 기능에 넣지 않는다.

## 14. 초기 개발 범위와 우선순위

### P0 — 게임과 점수 검증

- Quick, Draw! 기반 분류 모델 기준선 확보
- 실제 그림판 입력 전처리와 브라우저 실행 확인
- Pen / Undo / Reset / 제출 구현
- 지원 카테고리의 속성 데이터와 속성별 벡터 작성
- 속성별 코사인 유사도·보정·가중치와 사전 점수표 구성
- Top-3 전송, 후보별 관계 점수 합산, 후보 가중 평균과 내부 순위 기록
- 가중치에 따른 탐색 경험, 낮은 확신도 판정 보류, 결측 처리 검증
- 자동 성공 조건의 초기 검증

### P1 — 초기 서비스

- 데일리 문제와 서버 판정 API
- 제출 기록과 성공 화면
- 모바일·데스크톱 반응형 그림판, 썸네일·점수 기록, 하단 Q&A
- 중복 제출·통신 실패·모델 로딩 처리
- 문제별 모델·점수 체계 버전 연결
- 속성 모듈 등록 규격과 후보별·속성별 점수 내역 저장
- 학습용 수집 안내·선택과 그림 저장
- 미검수 상태 및 후속 라벨 저장을 위한 데이터 항목
- 실제 사용자 플레이 테스트

### P2 — 후속 확장

- 검수 도구와 데이터셋 관리
- 사용자 데이터 재학습·평가·배포 자동화
- 모델 개선 효과 관찰
- 환경·재질·색상 등 속성 모듈 추가와 가중치 조정
- 순위·예측 단어 공개 정책 조정
- 저사양 기기 성능 고도화, 힌트, 연습 모드 등 추가 검토

## 15. 초기 범위에서 제외하는 기능

- 멀티플레이, 방 코드, 실시간 경쟁, 동점 판정
- WebSocket 기반 그림 스트리밍과 매 획 추론
- 텍스트 정답 입력과 정답 입력 해금 조건
- 연속 프레임 기반 Stable Prediction
- GPU 동적 배치와 실시간 추론 큐 운영
- 자동 재학습, 검수 운영 도구, 자동 배포 파이프라인
- 계정, 친구, 영구 랭킹, 연속 출석 기록
- 복잡한 그림 도구와 리플레이

문자 입력을 그림으로 오인하는 문제는 시제품에서 확인한다. OCR과 문자 검출에 따른 실격 정책을 초기 필수 기능으로 확정하지 않는다.

## 16. 개발 전에 정리할 남은 결정

| 항목 | 결정 시점 | 초안의 처리 |
|---|---|---|
| 속성 구성·벡터 생성·보정·가중치 | 초기 벡터·점수표 작성과 플레이 테스트 | 속성별 코사인 비교·가중 합산 채택, 초기 권장 축은 분류·형태·기능 |
| Top-3 출력 보정·판정 보류 기준 | 검증 그림 평가 후 | 원래 Top-3 점수를 전송하고 서버에서 인식 가능 여부 확인 후 가중 평균 |
| 결측·영벡터·적용 불가 처리 | 모듈 등록 시 | 초기 핵심 데이터 완비, 후속 모듈 처리 규칙은 명시적으로 버전 관리 |
| 점수 범위·표시 자릿수 | 점수 분포 확인 후 | 예시 값만 사용 |
| 공동 순위와 반올림 규칙 | 점수표·표시 범위 설계 시 | Top-3 각 후보의 관계 순위 기록 |
| 혼합 점수의 단일 순위 | 순위 공개 검토 시 | 후보 순위와 구분. 필요성과 정의 미정 |
| 인식 가능 기준과 판정 보류 | 검증 그림 평가 후 | 낮은 확신도에 점수 미제공 권장 |
| 성공 임계값 | 검증 그림 평가 후 | 수치 미정 |
| 예측 단어·순위 공개 | 사용성 테스트 후 | 화면 옵션으로 분리 |
| 서버 재추론 | 초기 서버 설계 시 | 단순 판정과 성공 후보 재검사 비교 |
| 수집 범위·보관 정책 | 수집 기능 출시 전 | 성공 + 일부 실패 표본 권장 |
| 정답 목록 | 모델 기준선 평가 후 | 지원 카테고리 중 선별 |
| 포기 기능·자정 처리 | 화면·문제 API 설계 시 | 권장 기본안을 검토 |

## 17. 참고 자료

게임의 공개 도움말과 화면을 기준으로 2026-09-22 확인했다.

- [꼬맨틀 — 게임과 질문·답변](https://semantle-ko.newsjel.ly/)
- [뉴맨틀 — 게임과 도움말](https://www.newmantle.kr/)
- [Semantle — 게임과 FAQ](https://semantle.com/)
- [Contexto — 데일리 게임과 How to play](https://contexto.me/en/daily)
- [카맨틀 — 공식 소개](https://kamantle.com/)
- [LoLdle Classic — 속성 비교와 부분 일치 규칙](https://loldle.org/classic)
- [Metazooa — 생물 분류 기반 탐색 규칙](https://metazooa.com/)
- [Quick, Draw! — 공식 데이터 설명](https://github.com/googlecreativelab/quickdraw-dataset)
- [Quick, Draw! — 데이터 라이선스](https://github.com/googlecreativelab/quickdraw-dataset/blob/master/LICENSE)
- [ONNX Runtime Web — 브라우저 추론 안내](https://onnxruntime.ai/docs/tutorials/web/)
- [Google Cloud — MLOps 확장 참고 자료](https://docs.cloud.google.com/architecture/mlops-continuous-delivery-and-automation-pipelines-in-machine-learning)
