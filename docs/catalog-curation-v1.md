# 카테고리 정리 v1 — 겹치는 개념·구분 불가 항목

기준: [기획서 v3.2](<../AI 스케치 게임 서비스 기획서 v3.2.md>) §9.1("동의어 표기와 카테고리 중복·유사성은 목록 관리 단계에서 정리한다"), [라벨 검토](quickdraw-label-review.md), [속성 사전 v3](attribute-dictionary.md). 작성일: 2026-09-23.

결과 파일: [catalog-curation-v1.json](../config/model/catalog-curation-v1.json). 검토 반영 버전은 `catalog-curation-v1.1`이며 아직 초안이다. 원본 카탈로그 `labels.json`과 모델 클래스 순서는 바꾸지 않는다.

## 1. 결과 요약

| 항목 | 개수 |
|---|---:|
| 원본 모델 클래스 | 345 |
| 매핑 후 인식 후보 (미지원 포함) | 335 |
| 서비스 카테고리 (점수 지원 초안) | 334 |
| 대표 카테고리로 통합 | 10 |
| 인식 후보에서 제거 | 0 |
| 점수·정답 미지원 후보 | 1 (`bird`) |
| 데일리 정답 후보 | 323 |
| 통합 보류 | 6쌍 |

데일리 정답 후보는 서비스 카테고리 334개에서 정답 제외 11개를 뺀 수다. 통합 보류한 6개 원본은 독립 후보로 복원했다. 인식 품질 검증 전이므로 출시 승인 목록이 아니다. 이전 328/317 집계와 설정은 [검토 이력](../data/attributes/draft-v3/review-corrections-2026-09-23.json)에 보존했다.

## 정답에서만 제외하는 항목

기존 라벨 검토는 도형·기호·추상 활동 13개를 정답 제외로 제안했다(`exclude_proposed`). 이 중 별 모양과 웃는 얼굴 기호는 정답 후보로 다시 넣는다. 나머지 11개는 그대로 정답에서 제외한다. 제외된 11개도 점수는 계산한다. 사용자가 그렸을 때 인식하고 유사도를 보여주되 오늘의 정답으로만 내지 않는다.

| 구분 | 카테고리 | 이유 |
|---|---|---|
| 정답 후보로 되돌림 | 별 모양 `star`, 웃는 얼굴 기호 `smiley_face` | 누구나 같은 모양으로 알아보고 그리기 쉬운 대표 기호다. 사용자 결정(2026-09-23). |
| 정답 제외 유지 | 원, 육각형, 팔각형, 정사각형, 삼각형, 직선, 구불구불한 선, 지그재그 | 구체적인 대상이 아닌 기하 도형이다 |
| 정답 제외 유지 | 동물의 이동, 위장, 요가 | 대상이 아닌 동작·추상 개념이다 |

원본 `labels.json`의 `daily_status`는 바꾸지 않았다. 되돌린 두 항목은 정리 파일의 `daily_overrides`에 기록했다.

## 2. 판단 기준

카테고리 쌍은 아래 순서로 판단했다.

| 기준 | 처리 | 예 |
|---|---|---|
| **같은 개념·하위 종류** (`same_or_subtype`) | 대표 개념으로 인정하는 규칙 초안 유지 | 머그잔→컵 |
| **부분을 전체로 인정** (`part_as_whole`) | 게임 규칙 초안으로 유지, 출시 전 검증 | 발가락→발, 무릎→다리 |
| **별개 개념의 구분 불가 주장** (`no_sketch_difference`) | 통합 보류, 각 후보 유지 | 첼로–바이올린, 허리케인–토네이도 |
| **넓은 상위 개념이 여러 개별 카테고리와 겹침** (`broad_parent`) | 점수·정답 제외, 인식 후보와 확률은 유지 | 새 |
| 개념이 다르고 그림에서 구분되는 특징이 있음 | 둘 다 유지한다 | 사자–호랑이, 노트북–컴퓨터 |

대표는 다음 순서로 골랐다.

1. 더 일반적이고 한국어로 익숙한 이름 (컵, 케이크, 시계)
2. 전체 개념 (발, 팔, 다리)
통합은 하위 종류·부분 그림도 대표 정답으로 인정하겠다는 규칙이다. 태그 유사도나 모델 혼동 자체가 이 규칙을 승인하는 근거는 아니다.

태그 유사도가 높다는 것만으로 합치지 않는다. v3 태그는 비슷한 대상의 점수가 가깝게 나오도록 일부러 공유하게 만들었기 때문이다. 예를 들어 사자–호랑이(0.89)는 가까운 점수가 나와야 하는 별개 대상이다.

근거 자료:

- **태그 유사도**: [속성 사전 v3](../data/attributes/draft-v3/dictionary.json)에서 분류·형태·기능 축별 태그 코사인의 평균. 두 대상 모두 태그가 있는 축만 쓴다.
- **그림 표본**: [Quick Draw 미리보기](../data/quickdraw/previews/index.html)의 라벨별 20장
- **기존 라벨 검토**의 우선 검토 목록

인식 모델의 혼동 행렬은 아직 근거에 없다(6절).

## 3. 적용 방식

| 영역 | 처리 |
|---|---|
| 모델 출력 | 345개 logits 전체에 온도 보정·softmax를 적용한다. 원본 확률을 `service_id`별로 합산한 335개 후보에서 Top-3를 뽑는다. `bird`도 유지하고 마스킹·재정규화하지 않는다. |
| 제출 요청 | Top-3의 `categoryId`는 매핑 후 후보 ID다. 통합된 원본 ID는 대표 ID로 보내며 미지원 `bird`는 그대로 보낸다. p는 전체 확률 질량을 유지한 합산 값이다. |
| 미지원 후보 | Top-3에 `bird` 등 점수 미지원 후보가 하나라도 있으면 `deferred/unsupported_candidate`. 나머지만 골라 점수를 내지 않는다. |
| 동점 순서 | 후보에 합산되는 원본 `class_index` 중 최솟값을 `candidate_index`로 삼고, 같은 p일 때 이 값의 오름차순으로 정렬한다. |
| 점수표 | 대표 카테고리만 점수를 만든다. 통합된 원본의 v3 속성 레코드는 보존하되 점수에 쓰지 않는다. |
| 데일리 정답 | 대표 카테고리만 후보가 된다. 예를 들어 정답이 컵일 때 머그잔을 그려도 확률이 컵으로 합산되므로 성공할 수 있다. |
| 재학습 | 현재 학습 라벨 345개를 유지한다. 출력 라벨 변경은 별도 모델·카탈로그 버전과 재평가가 필요하다. |
| 버전 | 이 초안을 선택하는 릴리스의 `catalogVersion`은 `catalog-curation-v1.1`이다. 공개 manifest에 원본 순서·매핑·후보 순서·매핑 해시를 함께 고정하며 진행 중인 문제의 매핑은 바꾸지 않는다. |

예를 들어 원본 확률이 `bird=.98, owl=.018, duck=.001, parrot=.001`이면 `owl`은 여전히 `.018`이다. `bird`가 Top-1이므로 보류한다. `bird`를 지운 뒤 `owl=.9`로 만드는 방식은 사용하지 않는다. `q=p/sum(top3.p)`는 지원·확신도 검사를 통과한 뒤 유사도 혼합에만 사용한다.

확률 합산은 후보 수와 확신도 분포를 바꾸므로 보류·성공 임계값, 보정 품질, 혼동 행렬을 **매핑 적용 후 기준으로도** 평가한다. 원본 345개 평가 결과도 별도로 남긴다.

## 4. 통합·제외 목록

| 남김 | 뺌 | 기준 | 태그 유사도 | 근거 |
|---|---|---|---:|---|
| 컵 `cup` | 머그잔 `mug`, 커피잔 `coffee_cup` | 하위 종류 | 1.00 / 0.92 | 손잡이 달린 원통 잔이라는 형태·기능 태그가 같다. '컵'이 가장 일반적인 이름이다. |
| 버스 `bus` | 통학버스 `school_bus` | 하위 종류 | 1.00 | 용도 구분일 뿐이고 v3 태그 구성이 완전히 같다. |
| 트럭 `truck` | 픽업트럭 `pickup_truck` | 하위 종류 | 0.82 | 열린 짐칸 외에는 형태·기능이 트럭과 같다. |
| 케이크 `cake` | 생일 케이크 `birthday_cake` | 하위 종류 | 0.89 | 초를 꽂은 케이크다. 생일 케이크를 그려도 케이크로 인정되는 편이 자연스럽다. |
| 발 `foot` | 발가락 `toe` | 부분 인정 초안 | 0.90 | 부분 그림도 전체 정답으로 인정하는 규칙이며 구분 불가가 입증된 것은 아니다. |
| 팔 `arm` | 팔꿈치 `elbow` | 부분 인정 초안 | 0.90 | 같은 규칙을 적용하며 출시 전 검증한다. |
| 다리 `leg` | 무릎 `knee` | 부분 인정 초안 | 0.47 | 낮은 유사도의 원인을 태그 작성 차이로 단정하지 않는다. 부분 정답 인정과 실제 혼동을 별도로 검증한다. |
| 수염 `beard` | 턱수염 `goatee` | 하위 종류 | 0.70 | 털 가닥 뭉치라는 형태가 같고 둘 다 기능이 없다. |
| 시계 `clock` | 알람시계 `alarm_clock` | 하위 종류 | 0.64 | 알람시계를 그려도 시계로 인정되는 편이 자연스럽다. 손목시계는 착용품이라 유지한다. |
| 인식 후보 `bird` | 점수·정답에서만 제외 | 넓은 상위 개념 | 0.60~0.82 | 확률을 유지하고 Top-3에 있으면 보류한다. 다른 새로 확률을 넘기지 않는다. |

### 통합 보류: 각 ID를 독립적으로 유지

| 쌍 | 보류 근거 |
|---|---|
| 첼로–바이올린 | 별개 악기이며 크기·받침 등 구분 단서를 20장 표본만으로 배제할 수 없다. |
| 블랙베리–블루베리 | 작은 알갱이 표면과 둥근 열매가 스케치의 구분 단서가 될 수 있다. |
| 크레용–연필 | 태그 공유가 정답 상호 인정이나 시각적 구분 불가를 입증하지 않는다. |
| 여권–책 | 그림 중첩만으로 여권을 책 정답으로 인정하는 규칙을 확정하지 않는다. |
| 식기세척기–세탁기 | 식기·선반과 둥근 창 같은 구분 단서가 있다. |
| 허리케인–토네이도 | 원형 소용돌이와 깔때기 윤곽이 구분 단서가 될 수 있다. |

## 5. 검토 후 유지한 쌍

태그 유사도가 높거나 개념이 가까워도 구분 단서가 있어 둘 다 남긴 기존 표본이다. 통합 보류 6쌍은 앞 절에 별도로 기록했다. 전체 후보의 검증 완료 목록은 아니다.

| 쌍 | 태그 유사도 | 유지 근거 |
|---|---:|---|
| 기타–바이올린 | 0.96 | 둥근 울림구멍, 뜯는 연주 방식. 한국 사용자에게 별개 악기다. |
| 자동차–버스 | 0.96 | 차체 길이와 창문 줄로 구분된다. |
| 트롬본–트럼펫 | 0.92 | 긴 슬라이드와 밸브로 구분된다. |
| 승합차–트럭 | 0.90 | 사람을 태우는 높은 차체와 분리된 짐칸이 다르다. |
| 승합차–버스 | 0.90 | 버스보다 짧고 창문 줄이 적다. |
| 비–구름 | 0.90 | 빗방울이 함께 그려진다. |
| 포도–블루베리 | 0.90 | 포도는 역삼각형 송이와 꼭지로 그려진다. |
| 마커–연필 | 0.89 | 뚜껑이 있는 굵은 몸체다. |
| 노트북–컴퓨터 | 0.89 | 접힌 화면+자판과 분리된 모니터가 다르다. |
| 화분 식물–꽃 | 0.89 | 화분이 그려진다. |
| 사자–호랑이 | 0.89 | 갈기와 줄무늬. 가까운 점수는 의도한 결과다. |
| 하키 스틱–골프채 | 0.89 | 꺾인 날과 곧은 샤프트 끝 헤드가 다르다. |
| 전자레인지–오븐 | 0.86 | 창문 문+버튼판과 앞문+내부 선반이 다르다. |
| 경찰차–자동차 | 0.81 | 경광등. 구급차·소방차와 함께 긴급 차량으로 구분된다. |
| 판다–곰 | 0.81 | 눈가·팔다리 무늬. |
| 천장 선풍기–선풍기 | 0.78 | 매달린 구조와 받침 위 망 구조가 다르다. |
| 레인지–오븐 | 0.77 | 윗면 화구와 불꽃이 있다. |
| 콧수염–수염 | 0.75 | 코 아래와 턱이라는 위치와 모양이 다르다. |
| 손가락–손 | 0.61 | 손가락 하나만 길게 그리는 경우가 많다. |

## 6. 한계와 다음 확인

- 근거는 태그·그림 표본·기존 검토다. **인식 모델의 혼동 행렬로 검증하지 않았다.** 64px 모델 학습 후 두 가지를 다시 확인해 `catalog-curation-v2`를 만든다.
  - 태그는 다르지만 그림이 비슷한 쌍 (예: 프라이팬–테니스 라켓)
  - 5절에서 유지한 쌍 중 실제로 구분되지 않는 쌍
- 태그 유사도는 태그 작성 방식에 좌우된다. 무릎–다리의 통합은 구분 불가 판정이 아닌 부분 정답 인정 초안이다.
- 컵류·케이크·시계의 대표 처리와 새의 보류 방식을 정했다. 통합 보류 6쌍과 부분 정답 인정 규칙은 모델 평가·게임 규칙 검증 대상으로 남긴다. 그 밖에 남은 라벨 검토:
  - `fish`
  - `diamond`, `stitches`, `fan`, `spreadsheet`
  - 고유 명소·작품 3개
- 이 정리는 정답·점수 목록을 줄이는 것이다. 모델 학습 데이터를 지우는 것이 아니다.
